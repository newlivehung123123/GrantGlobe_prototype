# Scrapy configuration for GrantGlobe Stage 2 crawler
# https://docs.scrapy.org/en/latest/topics/commands.html#deploy-target

[settings]
default = grantglobe_crawler.settings

[deploy]
# project name used by scrapyd (if deployed to a scrapyd server)
project = grantglobe_crawler
